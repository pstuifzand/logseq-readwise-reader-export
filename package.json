{
  "name": "logseq-readwise-reader-export",
  "version": "0.1.0",
  "description": "Readwise Reader export",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-source-maps index.html --public-url ./",
    "watch": "parcel watch index.html --public-url ./"
  },
  "keywords": [],
  "author": "Peter Stuifzand",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "0.0.10"
  },
  "logseq": {
    "id": "logseq-readwise-reader-export",
    "title": "Readwise Reader Export",
    "icon": "./icon.png"
  },
  "devDependencies": {
    "parcel": "^2.0.0"
  }
}
